<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <include layout="@layout/include_common_navigation" />

    <include layout="@layout/include_common_remind" />

    <include layout="@layout/include_common_network_status" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">


        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@color/white"
            android:orientation="vertical"
            android:padding="@dimen/distance_view">

            <EditText
                android:id="@+id/etValue"
                style="@style/EditStyle"
                android:hint="请输入昵称"
                android:visibility="gone" />

            <com.znz.compass.znzlibray.views.EditTextWithLimit
                android:id="@+id/etValueMulti"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:visibility="gone"
                app:limit_hint="请输入您的个性签名"
                app:limit_size="28" />
        </LinearLayout>

        <TextView
            android:id="@+id/tvSubmit"
            style="@style/BtnStyle"
            android:layout_margin="@dimen/distance_view"
            android:text="保存"
            android:textColor="@color/white" />


    </LinearLayout>

</LinearLayout>